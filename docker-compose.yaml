version: '3.7'

services:
  mongo:
    container_name: mongo
    image: mongo:4.0.9-xenial
    ports:
      - 27017:27017
    restart: always
    volumes:     
      - mongo-data:/data/db

volumes:
  mongo-data: